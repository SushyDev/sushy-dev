document.addEventListener('mousemove', () => import('./client.js'), { once: true });
document.addEventListener('touchstart', () => import('./client.js'), { once: true });
document.addEventListener('scroll', () => import('./client.js'), { once: true });
